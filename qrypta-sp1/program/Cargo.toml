[package]
name = "program"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = "5.2.4"
sha2 = { version = "0.10", default-features = false }

[profile.release]
panic = "abort"
lto = true
opt-level = 3
